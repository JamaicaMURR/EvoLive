using UnityEngine;
using System.Collections;

public class Tester : MonoBehaviour
{
    //public int iterations = 0;
    //public Rotator r;
    //public Mover m;
    //public Replicator rep;
    //public Injector inj;
    //public Pusher pus;
    //public EnergyGiver giv;
    //public Equalizer eq;

    //private int i = 0;

    //void Start()
    //{

    //}

    //void Update()
    //{
    //    if(i < iterations)
    //    {
    //        //if(Input.GetKeyDown(KeyCode.Space))
    //        //    r.Rotate(new float[] { 0.5f, 0.2f, 0.2f });

    //        if(Input.GetKeyDown(KeyCode.R))
    //            rep.AbsReplicateForward(0, 3, 0.5f, true, 0, false);

    //        if(Input.GetKeyDown(KeyCode.I))
    //            inj.RelInject(0, 0);

    //        if(Input.GetKeyDown(KeyCode.P))
    //            pus.RelPush(0);

    //        if(Input.GetKeyDown(KeyCode.G))
    //            giv.RelGive(0, 0.3f);

    //        if(Input.GetKeyDown(KeyCode.E))
    //            eq.RelEqualize(0);

    //        //rep.Replicate(new float[] { 0.5f, 0.5f, 0.5f });
    //    }

    //    //i++;
    //}
}
